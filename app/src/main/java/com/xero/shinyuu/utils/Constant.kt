package com.xero.shinyuu.utils

//the user is node, so we write use_node in caps
//wherever we used user in kotlin class(signUpActivity.kt), we change it to USER_NODE.
const val USER_NODE = "User"
const val USER_PROFILE_FOLDERS = "Profile"
const val POST_FOLDER = "PostImages"
const val POST = "Post"
const val CONNECT = "Connect"